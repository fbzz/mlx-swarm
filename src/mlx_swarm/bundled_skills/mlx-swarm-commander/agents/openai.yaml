interface:
  display_name: "MLX Swarm Commander"
  short_description: "Route, plan, and review local MLX Swarm work"
  default_prompt: "Use $mlx-swarm-commander to decide whether this objective needs Swarm; route simple changes to direct implementation, otherwise prepare a validated two-agent local plan."
